# -*- coding: utf-8 -*-

DEBUG = False

# Database
# https://docs.djangoproject.com/en/1.11/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'audio-books-player',
        'USER': 'audio-player',
        'PASSWORD': 'audio',
        'HOST': '127.0.0.1',
        'PORT': '5432'
    }
}

ALLOWED_HOSTS = [
    '127.0.0.1'
]

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = ''